<div class="row background">
  <h1 class="small-12 columns"><%= @user.name %></h1>
  <div class="small-12 large-4 columns">
    <img src=<%= @user.avatar.file.nil? ? default_avatar_path : @user.avatar.url %> />
  </div>
  <div class="small-12 large-8 columns">
    <p>Bio</p>
    <p><%= @user.bio %></p>
  </div>

</div>
